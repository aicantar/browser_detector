/// Detectable browsers.
enum Browsers {
  chrome,
  firefox,
  safari,
  edge,
  unknown,
}

/// Detectable browser engines.
enum Engines {
  blink,
  gecko,
  webkit,
  edgeHTML,
  unknown,
}

/// Detectable platforms.
enum Platforms {
  windows,
  macOS,
  linux,
  unix,
  unknown,
}
